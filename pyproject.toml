[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "codeyard_custom"
authors = [
    { name = "Code Yard Private Limited", email = "tech@codeyard.app" }
]
description = "Custom Frappe app for Codeyard - meeting room booking, internal tools, and customizations"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[tool.bench.frappe-dependencies]
frappe = ">=16.0.0"
erpnext = ">=16.0.0"
